import { pg_colors } from '../../themes/pg-tailwindcss/tokens.mjs'

const primary = pg_colors.primary.DEFAULT
const secondary = pg_colors.secondary.DEFAULT

const safelist = []

export { primary, secondary, safelist }
